spring:
  h2:
    console:
      enabled: true
      path: /h2
      setting:
        web-allow-others: true
  datasource:
    url: jdbc:h2:mem:test
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true